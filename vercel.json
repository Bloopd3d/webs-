{
    "buildCommand": "cd frontend 2>/dev/null || : && yarn install && CI=false yarn build && cd .. && rm -rf build && mv frontend/build build",
    "outputDirectory": "build",
    "framework": null,
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}