{
  "name": "restaurant-landing-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend 2>/dev/null || : && yarn install && CI=false yarn build && cd .. && rm -rf build && mv frontend/build build"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "yarn@1.22.22"
}